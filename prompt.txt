I have a federated learning project running with Flower + Opacus + FedProx. The training logs are being generated in real-time from a server and multiple hospital clients. I need you to do the following:
STEP 1: Parse all available data sources
Look through the entire project directory structure. Find:

fl_metrics.json (server saves per-round metrics here)
Any other JSON, CSV, or log files generated by the server or clients
The server code (likely server.py) to understand what metrics are being logged and how
The client code (likely client.py) to understand training configuration
Any configuration files that specify: model architecture, dataset used, FedProx mu value, learning rate, batch size, number of rounds, number of local epochs, noise multiplier, clipping norm, epsilon target, delta value
The model definition file to confirm the architecture (what CNN/ResNet variant, GroupNorm details)
Any inference or Grad-CAM code that generates heatmaps
The data directory structure to understand how data is split between hospitals (check demo/Hospital_A/local_storage/data and demo/Hospital_B/local_storage/data — report the number of images per class per hospital)

STEP 2: If training is still running, wait or work with what's available. If training is complete, proceed.
STEP 3: Create a folder called paper_data/ in the project root. Inside it, generate the following CSV files:

round_metrics.csv — One row per federated round with columns:

round_number
global_val_accuracy
global_test_accuracy
global_val_f1
global_test_f1
hospital_a_test_accuracy
hospital_a_test_f1
hospital_a_epsilon_spent
hospital_b_test_accuracy
hospital_b_test_f1
hospital_b_epsilon_spent
aggregated_epsilon
aggregated_delta
fairness_gap
fairness_score
risk_level


training_config.csv — Single row with columns:

model_architecture (exact class name and layer details)
dataset_name
total_rounds
local_epochs
batch_size
learning_rate
optimizer
fedprox_mu
noise_multiplier
max_grad_norm (clipping)
target_epsilon
target_delta
num_clients
hospital_a_total_samples
hospital_a_normal_count
hospital_a_pneumonia_count
hospital_b_total_samples
hospital_b_normal_count
hospital_b_pneumonia_count


loss_per_batch.csv — Parse from terminal logs if no other source exists. Columns:

round_number
hospital_name
epoch
batch_number
total_batches
loss_value


privacy_tracking.csv — Per round per hospital. Columns:

round_number
hospital_name
epsilon_spent
delta
risk_level



STEP 4: Generate the following plots and save them as high-resolution PNG files (300 DPI, white background, clean fonts) in paper_data/plots/:

convergence_accuracy.png — Line plot: X-axis = round number, Y-axis = accuracy. Three lines: Global Test Accuracy, Hospital_A Test Accuracy, Hospital_B Test Accuracy. Include legend, grid, axis labels.
convergence_f1.png — Same as above but for F1 scores.
convergence_loss.png — Line plot: X-axis = epoch (continuous across rounds), Y-axis = loss. Two lines: Hospital_A average epoch loss, Hospital_B average epoch loss. Show round boundaries as vertical dashed lines.
privacy_budget.png — Line plot: X-axis = round number, Y-axis = cumulative epsilon spent. Three lines: Hospital_A, Hospital_B, Aggregated.
fairness_gap.png — Line plot: X-axis = round number, Y-axis = fairness gap between hospitals. Add a horizontal reference line at 0.1 labeled "Target Fairness Threshold".
per_hospital_comparison.png — Grouped bar chart: For the final round, show accuracy and F1 side by side for Hospital_A vs Hospital_B vs Global.
training_loss_heatmap.png — Heatmap: X-axis = epoch within round, Y-axis = round number, color = average loss. One heatmap per hospital, side by side.

STEP 5: Create a paper_data/summary.txt file containing:

Total number of rounds completed
Final global accuracy and F1
Final per-hospital accuracy and F1
Total privacy budget consumed per hospital
Final fairness gap
Whether accuracy improved, plateaued, or degraded over rounds
The model architecture summary (number of parameters, layers)
Dataset split summary (samples per hospital per class)
Any anomalies observed (e.g., one hospital significantly worse than another, loss spikes, privacy budget exceeded)

STEP 6: If Grad-CAM / explainability code exists in the project:

Run inference on 3 sample pneumonia images and 3 sample normal images
Save the Grad-CAM heatmap overlays in paper_data/gradcam/
Name them pneumonia_1.png, pneumonia_2.png, pneumonia_3.png, normal_1.png, normal_2.png, normal_3.png

STEP 7: Check if there's any way to run the same training with these variations (if config allows easy switching):

A run with noise_multiplier=0 (no DP) — save results as round_metrics_no_dp.csv
A run with standard FedAvg instead of FedProx — save results as round_metrics_fedavg.csv
If these comparison runs are possible, generate additional plots:

dp_vs_no_dp_accuracy.png — comparing accuracy with and without differential privacy
fedprox_vs_fedavg_accuracy.png — comparing FedProx vs FedAvg convergence



If these comparison runs are NOT easily configurable, skip this step and note it in summary.txt.
IMPORTANT NOTES:

Use matplotlib with a clean academic style (no dark backgrounds, use plt.style.use('seaborn-v0_8-whitegrid') or similar)
All plots should have proper axis labels, titles, and legends
Use consistent colors across all plots (e.g., blue for Global, green for Hospital_A, red for Hospital_B)
Parse terminal/console logs as a fallback if structured log files don't exist
Do NOT fabricate any data. If a metric is not available, leave it blank and note it in summary.txt
Print a final checklist of what was generated and what's missing


